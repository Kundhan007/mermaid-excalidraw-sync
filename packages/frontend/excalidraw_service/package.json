{
  "name": "@mermaid-excalidraw-sync/excalidraw-service",
  "version": "0.1.0",
  "description": "Mermaid to Excalidraw converter core",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/*"
  ],
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "rimraf -rf ./dist && cross-env tsc -b src",
    "test": "vitest run",
    "test:coverage": "vitest --coverage",
    "test:watch": "vitest --watch"
  },
  "dependencies": {
    "@excalidraw/markdown-to-text": "0.1.2",
    "@mermaid-js/parser": "^0.6.3",
    "mermaid": "^11.12.1",
    "nanoid": "4.0.2"
  },
  "devDependencies": {
    "@types/mermaid": "^9.2.0",
    "@vitest/coverage-v8": "^1.6.0",
    "cross-env": "7.0.3",
    "jsdom": "^24.0.0",
    "rimraf": "5.0.5",
    "typescript": "5.2.2",
    "vitest": "^1.6.0"
  }
}
